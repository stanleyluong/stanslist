# Stan's List - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit your actual .env file to version control!

# Firebase Configuration
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:abcdef123456789012345
FIREBASE_MEASUREMENT_ID=G-ABCDEFGHIJ

# Google Services
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
GOOGLE_SIGN_IN_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_SIGN_IN_SERVER_CLIENT_ID=your_server_client_id.apps.googleusercontent.com

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual configuration
# 3. Source the file before running deployment test: source .env
# 4. Add .env to your .gitignore to keep secrets safe
# 5. In AWS Amplify, add these same variables in the console under App Settings > Environment Variables

# Security Notes:
# - Never commit .env files to version control
# - Use different API keys for development and production
# - Regularly rotate your API keys
# - Ensure Firebase security rules are properly configured
# - Use least-privilege principle for all API keys
